import { styled } from "styled-components"

const NotesHeading = styled.h1`
    color: orange;
`

const Notes = () => {return(
    <div>
        <NotesHeading>
            Notes
        </NotesHeading>
    </div>
)}

export default Notes;
